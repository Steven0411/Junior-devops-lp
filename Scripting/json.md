# JSON

## Notation

JSON is built on two structures:
* A collection of name/value pairs.
* An ordered list of values.

Json object is an unordered set of name value pairs
e.g. 
json{ 
    key1: value1,
    key2: value2
}

Json array
e.g.
json{
    key1: [value1, value2, values3]
}